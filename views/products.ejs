<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Products</title>
    <link rel="stylesheet" href="./css/index.css">
    <link rel="stylesheet" href="./css/products.css">
    <link rel="stylesheet" href="./css/bootstrap/bootstrap.min.css">
</head>
<body>
    <%- include('../partials/header.ejs') %>
    <div class="main">
        <h1>Products</h1>
    <div class="products">
    <% for (let i = 0; i < products.length; i++) { %>
        <div class="product">
            <h3><%= products[i].name %></h3>
            <img src="<%= products[i].imageURL %>" alt="image <%= i+1 %>" >
            <p class="description"><%= products[i].description %></p>
            <p class="price">Price: $<%= products[i].price %></p>
            <p class="quantity">Quantity: <%= products[i].quantity %></p>
            <div class="quantitySelector">
                <button class="minusItem">-</button><input type="text" class="quantityInput" placeholder="0"><button class="plusItem">+</button>
            </div>
            <a href="/products/add/<%=products[i].id%>"><button class="addToCart">Add to cart</button></a>
        </div>
    <% } %>

    </div>
</body>
</html>